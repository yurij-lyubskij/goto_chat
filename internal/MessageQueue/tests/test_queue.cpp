#include <gtest/gtest.h>

#include <MessageQueue.h>

//TEST(queue, default) {
//    User user;
//    std::string cnt;
//    std::shared_ptr<iMessage> msg (new Message(cnt, user));
//    MessageQueue mQueue;
//    mQueue.Push(msg);
//    auto retval = mQueue.Pop();
//    EXPECT_EQ(msg, retval);
//}